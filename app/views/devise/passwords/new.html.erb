<h2 class="py-3 text-center text-white mt-5">Forgot your password?</h2>

  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="d-flex justify-content-center form-inline text-white">
    <%= f.error_notification %>

    <div class="form-inputs m-2">
      <%= f.input :email,
                  required: true,
                  autofocus: true,
                  placeholder: "Type your email",
                  input_html: { autocomplete: "email",
                  class:"m-4" } %>
    </div>

    <div class="form-actions ">
      <%= f.button :submit, "Send me reset password instructions", class:"btn btn-outline-light m-2"%>
    </div>
  <% end %>
</form>

<div class="m-3 text-center">
  <%= render "devise/shared/links"%>
</div>
