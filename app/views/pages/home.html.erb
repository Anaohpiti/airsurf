<div class="banner image-position">
  <div class="navbar navbar-home">
    <div>Logo</div>
    <div class="right-nav justify-content-right">
      <div class="log-buttons">
        <%= link_to "Sign Up", boards_path, class: "btn btn-primary btn-home" %>
        <%= link_to "Login", boards_path, class: "btn btn-light btn-home" %>
      </div>
    </div>
  </div>

  <div class="home-banner">
    <div class="content text-center">
      <h1>Airsurf</h1>
      <p>You want to go surfing ? Rent it !</p>
      <!--<% if false %>
        <%= simple_form_for @boards do |f| %>
          <%= f.input :name %>
          <%= f.submit "Find your board", class:"btn btn-primary" %>
        <% end %>
      <% end %> -->
      <%= link_to "Find your board", boards_path, class: "btn btn-primary btn-home" %>
    </div>
  </div>
</div>
